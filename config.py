port = "5432"
database = "postgres"
user = "postgres"

# These two values are read from User's home directory.
# Create a file ".pgpass" in the home directory with the following contents:
# host = "<host>"
# password = "<password>"
